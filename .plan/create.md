# 회의록 자동 저장 시스템 개발 사양서

## 📋 프로젝트 개요

### 목표
- 실시간 음성 인식 기반 회의록 자동 작성 시스템
- Windows/macOS 크로스 플랫폼 지원
- 완전 오프라인 작동 가능
- 화자 자동 구분 및 수동 편집 기능

### 핵심 요구사항
- ✅ 실시간 회의 정보 출력
- ✅ 한국어 주로 사용 (다국어 지원)
- ✅ 화자 정보 자동 구분 (AI 기반)
- ✅ 회의 시작과 함께 자동 저장
- ✅ 제목, 회의실 정보, 참여자 정보 관리
- ✅ 화자 정보 수동 수정 가능
- ✅ 완료된 내용 실시간 편집 가능
- ✅ Windows EXE 단독 실행 파일
- ✅ 추가 패키지 설치 없이 실행 (All-in-One)

---

## 🏗️ 시스템 아키텍처

### 전체 구조
```
┌─────────────────────────────────────────────┐
│            Electron Desktop App              │
├─────────────────────────────────────────────┤
│  Renderer Process (React UI)                │
│  - 실시간 자막 표시                           │
│  - 화자 구분 UI                               │
│  - 편집 인터페이스                            │
├─────────────────────────────────────────────┤
│  Main Process (Node.js)                     │
│  - Audio Capture (마이크/시스템 오디오)       │
│  - IPC 통신 관리                              │
│  - 파일 시스템 접근                           │
├─────────────────────────────────────────────┤
│  whisper.cpp Engine                         │
│  - 음성 → 텍스트 변환                         │
│  - CPU 최적화                                │
│  - macOS CoreML 지원                        │
├─────────────────────────────────────────────┤
│  Python Runtime (임베디드)                   │
│  - pyannote.audio (화자 분리)                │
│  - PyTorch CPU                              │
├─────────────────────────────────────────────┤
│  SQLite Database (Local)                    │
│  - 회의 정보 저장                             │
│  - 발화 내용 저장                             │
│  - 참여자 정보 관리                           │
└─────────────────────────────────────────────┘
```

---

## 💻 기술 스택

### Core Technologies

#### Frontend
- **Framework**: Electron 28.x
- **UI Library**: React 18.x
- **State Management**: Context API / Zustand
- **Styling**: Tailwind CSS / Styled Components
- **Build Tool**: Vite / Webpack

#### Backend (Main Process)
- **Runtime**: Node.js 20.x
- **Database**: better-sqlite3
- **Audio Processing**: node-record-lpcm16, sox-audio
- **IPC**: Electron IPC (contextBridge)

#### AI/ML Engines

##### Speech-to-Text
- **Engine**: whisper.cpp
- **Model**: medium (한국어 최적화)
- **Size**: ~160MB
- **특징**:
  - CPU 최적화 (4배 빠름)
  - macOS CoreML 지원 (10배 빠름)
  - 메모리 효율적 (~800MB)
  - 크로스 플랫폼

##### Speaker Diarization
- **Engine**: pyannote.audio 3.1
- **Model**: speaker-diarization-3.1
- **Runtime**: Python 3.11 (임베디드)
- **Dependencies**:
  - PyTorch 2.1.0+cpu (~180MB)
  - onnxruntime 1.16.0
- **특징**:
  - 한국어 회의 특성 대응 (동시 발화, 추임새)
  - 2-10명 화자 자동 구분
  - 정확도 85-90%

#### Packaging
- **Tool**: electron-builder 24.x
- **Target**:
  - Windows: NSIS Installer
  - macOS: DMG, ZIP
- **Total Size**: ~560MB (설치 후)
- **Installer Size**: ~350MB (압축)

---

## 📊 데이터베이스 스키마

### ERD
```
meetings
├── id (INTEGER, PK)
├── title (TEXT)
├── room (TEXT)
├── start_time (DATETIME)
└── end_time (DATETIME)

participants
├── id (INTEGER, PK)
├── meeting_id (INTEGER, FK)
├── name (TEXT)
└── speaker_label (TEXT) -- SPEAKER_00, SPEAKER_01 등

transcripts
├── id (INTEGER, PK)
├── meeting_id (INTEGER, FK)
├── speaker_id (INTEGER, FK)
├── text (TEXT)
├── start_time (REAL) -- 초 단위
├── end_time (REAL)
├── confidence (REAL) -- 인식 신뢰도
├── is_edited (BOOLEAN)
└── edited_text (TEXT)
```

### SQL Schema
```sql
CREATE TABLE meetings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  room TEXT,
  start_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  end_time DATETIME
);

CREATE TABLE participants (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  meeting_id INTEGER NOT NULL,
  name TEXT NOT NULL,
  speaker_label TEXT NOT NULL,
  FOREIGN KEY (meeting_id) REFERENCES meetings(id) ON DELETE CASCADE
);

CREATE INDEX idx_participants_meeting ON participants(meeting_id);
CREATE INDEX idx_participants_label ON participants(meeting_id, speaker_label);

CREATE TABLE transcripts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  meeting_id INTEGER NOT NULL,
  speaker_id INTEGER,
  text TEXT NOT NULL,
  start_time REAL NOT NULL,
  end_time REAL NOT NULL,
  confidence REAL,
  is_edited BOOLEAN DEFAULT 0,
  edited_text TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (meeting_id) REFERENCES meetings(id) ON DELETE CASCADE,
  FOREIGN KEY (speaker_id) REFERENCES participants(id) ON DELETE SET NULL
);

CREATE INDEX idx_transcripts_meeting ON transcripts(meeting_id);
CREATE INDEX idx_transcripts_time ON transcripts(meeting_id, start_time);
```

---

## 📁 프로젝트 구조

```
meeting-transcription-app/
├── package.json
├── electron-builder.json
├── README.md
├── LICENSE.txt
│
├── app/
│   ├── main.js                    # Electron 메인 프로세스
│   ├── preload.js                 # IPC 브릿지
│   │
│   ├── modules/
│   │   ├── audio-capture.js       # 오디오 녹음
│   │   ├── whisper-engine.js      # STT 엔진
│   │   ├── python-manager.js      # Python 런타임 관리
│   │   ├── database.js            # SQLite 관리
│   │   └── meeting-recorder.js    # 통합 회의록 로직
│   │
│   ├── renderer/
│   │   ├── index.html
│   │   ├── main.jsx               # React 엔트리
│   │   ├── App.jsx
│   │   │
│   │   ├── components/
│   │   │   ├── MeetingControl.jsx # 회의 시작/중지
│   │   │   ├── TranscriptView.jsx # 실시간 자막
│   │   │   ├── SpeakerManager.jsx # 화자 관리
│   │   │   ├── TranscriptEditor.jsx # 텍스트 편집
│   │   │   └── MeetingList.jsx    # 회의 목록
│   │   │
│   │   ├── contexts/
│   │   │   └── MeetingContext.jsx
│   │   │
│   │   ├── hooks/
│   │   │   ├── useMeeting.js
│   │   │   ├── useTranscript.js
│   │   │   └── useAudio.js
│   │   │
│   │   └── styles/
│   │       ├── global.css
│   │       └── components.css
│   │
│   └── resources/
│       ├── icon.png
│       └── icon.ico
│
├── python-runtime/                # 임베디드 Python (빌드 시 생성)
│   ├── python.exe (Windows)
│   ├── python3 (macOS)
│   └── Lib/
│       └── site-packages/
│           ├── torch/
│           ├── pyannote/
│           └── ...
│
├── python-scripts/                # Python 스크립트
│   ├── check_imports.py           # 환경 체크
│   ├── diarize.py                 # 화자 분리 실행
│   └── requirements.txt
│
├── whisper/                       # whisper.cpp (빌드 시 생성)
│   ├── main (macOS)
│   ├── main.exe (Windows)
│   └── models/
│       ├── ggml-medium.bin
│       └── ggml-medium-encoder.mlmodelc (macOS CoreML)
│
├── build/                         # 빌드 리소스
│   ├── icon.ico
│   ├── icon.icns
│   ├── installer.ico
│   └── entitlements.mac.plist
│
├── scripts/                       # 빌드 스크립트
│   ├── setup-whisper.js           # whisper.cpp 설정
│   ├── setup-python.js            # Python 임베디드 설정
│   └── build.js                   # 통합 빌드
│
└── dist/                          # 빌드 결과물
    ├── win-unpacked/
    ├── 회의록 시스템 Setup 1.0.0.exe
    └── 회의록 시스템-1.0.0.dmg
```

---

## 🔧 개발 환경 설정

### Prerequisites
```bash
# Node.js 20.x
# Python 3.11
# Git
# Windows: Visual Studio Build Tools
# macOS: Xcode Command Line Tools
```

### Installation
```bash
# 1. 저장소 클론
git clone <repository-url>
cd meeting-transcription-app

# 2. Node 의존성 설치
npm install

# 3. whisper.cpp 설정
npm run setup:whisper

# 4. Python 환경 설정
npm run setup:python

# 5. 개발 서버 시작
npm start
```

### package.json Scripts
```json
{
  "scripts": {
    "start": "electron .",
    "dev": "vite",
    "build": "vite build && electron-builder",
    "build:win": "npm run build -- --win",
    "build:mac": "npm run build -- --mac",
    "setup:whisper": "node scripts/setup-whisper.js",
    "setup:python": "node scripts/setup-python.js",
    "setup:all": "npm run setup:whisper && npm run setup:python",
    "test": "jest",
    "lint": "eslint app/"
  }
}
```

---

## 🎨 UI/UX 설계

### 주요 화면

#### 1. 메인 화면 (회의 시작)
```
┌─────────────────────────────────────────────┐
│  회의록 시스템                    [최소화][닫기]│
├─────────────────────────────────────────────┤
│                                             │
│  📝 새 회의 시작                             │
│                                             │
│  회의 제목: [________________]               │
│  회의실:   [________________]               │
│                                             │
│  [🎤 회의 시작하기]                          │
│                                             │
│  ─────────────────────────────────────      │
│                                             │
│  📁 최근 회의록                              │
│  • 2024년 1분기 실적 회의 (2시간 전)          │
│  • 마케팅 전략 회의 (어제)                    │
│  • 개발팀 스프린트 회의 (3일 전)              │
│                                             │
└─────────────────────────────────────────────┘
```

#### 2. 회의 진행 화면
```
┌─────────────────────────────────────────────┐
│  🔴 녹음 중 - 2024년 1분기 실적 회의   [■ 중지]│
├─────────────────────────────────────────────┤
│  시간: 00:15:32  |  화자: 3명  |  발화: 47개   │
├─────────────────────────────────────────────┤
│                                             │
│  [화자 A ▼] 00:00:05                         │
│  안녕하세요, 회의 시작하겠습니다.             │
│                                             │
│  [화자 B ▼] 00:00:12                         │
│  네, 준비됐습니다.                           │
│                                             │
│  [화자 A ▼] 00:00:18                         │
│  오늘 안건은 1분기 실적 검토입니다...         │
│  ┌────────────────────────────┐             │
│  │ 💬 실시간 음성인식 중...     │             │
│  │ "전년 대비 20% 증가했고"     │             │
│  └────────────────────────────┘             │
│                                             │
│  ▼ (자동 스크롤 중)                          │
│                                             │
├─────────────────────────────────────────────┤
│  [📤 내보내기] [⚙️ 설정]                     │
└─────────────────────────────────────────────┘
```

#### 3. 화자 관리 화면
```
┌─────────────────────────────────────────────┐
│  🎤 화자 관리                                │
├─────────────────────────────────────────────┤
│                                             │
│  화자 A (15개 발화)                          │
│  [김철수 부장________________] [✓]           │
│                                             │
│  화자 B (12개 발화)                          │
│  [이영희 과장________________] [✓]           │
│                                             │
│  화자 C (20개 발화)                          │
│  [박민수 대리________________] [✓]           │
│                                             │
│  [+ 화자 추가]                               │
│                                             │
│  ─────────────────────────────────────      │
│                                             │
│  💡 발화를 선택한 후 화자를 변경할 수 있습니다 │
│                                             │
│  [저장]  [취소]                              │
│                                             │
└─────────────────────────────────────────────┘
```

#### 4. 편집 화면
```
┌─────────────────────────────────────────────┐
│  ✏️ 회의록 편집                               │
├─────────────────────────────────────────────┤
│  제목: 2024년 1분기 실적 회의                 │
│  일시: 2024-12-12 14:00                      │
│  회의실: 3층 대회의실                         │
│  참여자: 김철수, 이영희, 박민수               │
├─────────────────────────────────────────────┤
│                                             │
│  [김철수 부장] 00:00:05        [수정] [병합]  │
│  안녕하세요, 회의 시작하겠습니다.             │
│  ────────────────────────────────────       │
│                                             │
│  [이영희 과장] 00:00:12        [수정] [병합]  │
│  네, 준비됐습니다.                           │
│  ────────────────────────────────────       │
│                                             │
│  [김철수 부장] 00:00:18        [수정] [분리]  │
│  ┌───────────────────────────────┐          │
│  │ 오늘 안건은 1분기 실적 검토입  │ [✓ 저장]  │
│  │ 니다. 전년 대비...             │ [✗ 취소]  │
│  └───────────────────────────────┘          │
│                                             │
│  ▼ 더보기                                    │
│                                             │
├─────────────────────────────────────────────┤
│  [💾 저장] [📋 복사] [📄 내보내기]             │
└─────────────────────────────────────────────┘
```

---

## 🔄 주요 워크플로우

### 1. 회의 시작 플로우
```
사용자 액션: "회의 시작" 버튼 클릭
    ↓
UI: 회의 정보 입력 폼 표시
    ↓
사용자: 제목, 회의실 입력
    ↓
Main Process: 
  1. SQLite에 회의 레코드 생성
  2. 오디오 캡처 시작
  3. 임시 파일에 녹음 (temp.wav)
    ↓
Renderer: 실시간 녹음 인디케이터 표시
    ↓
회의 중...
```

### 2. 실시간 음성인식 플로우
```
오디오 버퍼 (10초마다)
    ↓
Main Process: 오디오 청크 저장
    ↓
whisper.cpp: 음성 → 텍스트 변환
    ↓
결과: { start, end, text, confidence }
    ↓
IPC: Renderer에 전송
    ↓
React State 업데이트
    ↓
UI: 실시간 자막 표시 (자동 스크롤)
```

### 3. 화자 분리 플로우
```
회의 종료
    ↓
Main Process: Python 스크립트 호출
  python diarize.py temp.wav
    ↓
pyannote.audio: 화자 분리 실행
    ↓
결과: [
  { start: 0, end: 3, speaker: "SPEAKER_00" },
  { start: 4, end: 6, speaker: "SPEAKER_01" },
  ...
]
    ↓
Database: participants 테이블에 저장
    ↓
Matching: transcript와 speaker 매칭
  (시간대 기반)
    ↓
UI: 화자별 그룹핑 표시
```

---

## 🔌 IPC 통신 인터페이스

### Main → Renderer (이벤트)
```javascript
// 실시간 음성인식 결과
ipcRenderer.on('transcript-update', (event, data) => {
  // data: { start, end, text, confidence }
});

// 화자 분리 완료
ipcRenderer.on('diarization-complete', (event, data) => {
  // data: { speakers: [...], meetingId }
});

// 시스템 상태 업데이트
ipcRenderer.on('system-status', (event, data) => {
  // data: { recording, processing, error }
});

// 처리 진행상황
ipcRenderer.on('processing-progress', (event, data) => {
  // data: { stage, progress, message }
});
```

### Renderer → Main (핸들러)
```javascript
// 회의 시작
await ipcRenderer.invoke('start-meeting', {
  title: '회의 제목',
  room: '회의실'
});
// 반환: { meetingId, success }

// 회의 중지
await ipcRenderer.invoke('stop-meeting', {
  meetingId: 123
});
// 반환: { success, duration, transcriptCount }

// 화자 이름 업데이트
await ipcRenderer.invoke('update-speaker', {
  meetingId: 123,
  speakerLabel: 'SPEAKER_00',
  name: '김철수 부장'
});
// 반환: { success }

// 텍스트 편집
await ipcRenderer.invoke('edit-transcript', {
  transcriptId: 456,
  newText: '수정된 텍스트'
});
// 반환: { success }
```

---

## ⚡ 성능 최적화

### 노트북 (저사양) 최적화
```javascript
// 시스템 리소스 감지
const cpuCores = os.cpus().length;
const totalMemory = os.totalmem() / (1024 ** 3); // GB

// 동적 모델 선택
function selectOptimalModel() {
  if (totalMemory < 4 || cpuCores < 4) {
    return 'base'; // 가볍고 빠름
  } else if (totalMemory < 8 || cpuCores < 6) {
    return 'small'; // 균형잡힌 선택
  } else {
    return 'medium'; // 고품질
  }
}

// 스레드 수 조정
const threads = Math.max(2, cpuCores - 1);

// 청크 크기 조정 (메모리 절약)
const chunkSize = totalMemory < 4 ? 5000 : 10000; // ms
```

### macOS (Apple Silicon) 최적화
```javascript
// CoreML 자동 감지 및 활성화
function hasCoreML() {
  if (process.platform !== 'darwin') return false;
  
  const coremlPath = path.join(
    __dirname, 
    'whisper/models',
    'ggml-medium-encoder.mlmodelc'
  );
  
  return fs.existsSync(coremlPath);
}

// CoreML 사용시 설정
const whisperOptions = {
  modelPath: modelPath,
  useCoreML: hasCoreML(),
  threads: 4, // CoreML 사용시 더 적은 스레드
};
```

---

## 📦 빌드 및 배포

### 빌드 프로세스
```bash
# 1. 의존성 설치
npm run setup:all

# 2. 프로덕션 빌드
npm run build:win    # Windows
npm run build:mac    # macOS

# 3. 빌드 결과물
# dist/
#   ├── 회의록 시스템 Setup 1.0.0.exe (Windows)
#   └── 회의록 시스템-1.0.0.dmg (macOS)
```

---

## 🎯 마일스톤

### Phase 1: 기본 구조 (2주)
- ✅ Electron 앱 기본 구조
- ✅ SQLite 데이터베이스 설정
- ✅ 기본 UI 레이아웃
- ✅ 오디오 캡처 모듈

### Phase 2: 음성인식 통합 (2주)
- ✅ whisper.cpp 통합
- ✅ 실시간 음성인식 파이프라인
- ✅ 트랜스크립트 표시 UI
- ✅ 회의 시작/중지 기능

### Phase 3: 화자 분리 (2주)
- ✅ Python 런타임 임베딩
- ✅ pyannote.audio 통합
- ✅ 화자 매핑 로직
- ✅ 화자 관리 UI

### Phase 4: 편집 기능 (1주)
- ✅ 텍스트 인라인 편집
- ✅ 화자 이름 수정
- ✅ 발화 병합/분리
- ✅ Undo/Redo

### Phase 5: 고급 기능 (1주)
- ✅ 회의록 내보내기 (DOCX, TXT, SRT)
- ✅ 검색/필터
- ✅ 북마크/하이라이트

### Phase 6: 최적화 및 테스트 (2주)
- ✅ 성능 최적화
- ✅ 메모리 관리
- ✅ 단위 테스트
- ✅ E2E 테스트
- ✅ 버그 수정

### Phase 7: 패키징 및 배포 (1주)
- ✅ Windows 빌드
- ✅ macOS 빌드
- ✅ 설치 프로그램 테스트
- ✅ 문서 작성
- ✅ 릴리스

---

## 📚 참고 자료

### 기술 문서
- [Electron Documentation](https://www.electronjs.org/docs/latest)
- [whisper.cpp GitHub](https://github.com/ggml-org/whisper.cpp)
- [pyannote.audio Documentation](https://github.com/pyannote/pyannote-audio)
- [better-sqlite3 API](https://github.com/WiseLibs/better-sqlite3)
- [electron-builder Documentation](https://www.electron.build/)

---

**마지막 업데이트: 2024-12-12**